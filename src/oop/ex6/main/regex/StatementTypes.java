package oop.ex6.main.regex;

public enum StatementTypes {
    /**
     * enum of all statement types
     */
    DECLARE, ASSIGN, FINAL, IF, WHILE, METHOD_DECLARE, METHOD_CALL, RETURN, EMPTY_LINE, COMMENT, END_SCOPE
}
